use wasm_bindgen::prelude::*;

#[wasm_bindgen]
extern "C" {
  fn write_string(s: &str);
}

#[wasm_bindgen]
pub fn main() {
  write_string(&format!("{:?}", Program::new().CertiCoq_Benchmarks_tests_ack_3_9()))
}
